<% if logged_in? %>
  <div id="home">
    <home />
  </div>
  <div class="paginate">
    <% if @feed_items.any? %>
      <%= will_paginate @feed_items %>
    <% end %>
  </div>
<% else %>
  <%= render 'layouts/before_login' %>
<% end %>
