<% if logged_in? %>
  <div id="home">
    <home />
  </div>
  <div style="max-width: 600px; margin: 0 auto;">
    <% if @feed_items.any? %>
      <%= will_paginate @feed_items %>
    <% end %>
  </div>
<% else %>
  <%= render 'layouts/before_login' %>
<% end %>
